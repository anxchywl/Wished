:8000 {
    # route API requests to the backend container
    reverse_proxy /api/* backend:8000

    # route MinIO media requests
    reverse_proxy /wished-media/* minio:9000

    # route all other traffic to the frontend running on the host
    reverse_proxy * host.docker.internal:3000
}
